---
document_id: EUSTIA-PROJECT-ENTRY
status: active
canonical_filename: 项目入口.md
maintenance_mode: in_place_only
last_updated: 2026-08-11
storage_architecture: github_first
---

# 《秽翼的尤斯蒂娅》AI电影项目入口

> 这是项目唯一启动入口。2026-08-11 起，**GitHub 负责持续成长的文字主档；ChatGPT Library 负责媒体、大型母本、PDF与只读证据；Memory 只做辅助索引。** 当前迁移采用逐文件验证切换，尚未迁移完成的主档继续读取 File Library 当前 canonical，禁止依据旧文件搜索排名擅自切换。

# 一、每轮 P0 强制机制

每轮对话都必须执行：

1. 读取 GitHub `PROJECT_INDEX.yaml`，确认当前 Source Authority 与各主档 effective source。
2. 检索与本轮有关的长期记忆与项目记忆。
3. 执行《反馈反推与系统反哺引擎》学习闭环。
4. AI电影系统先做任务路由，只加载当前任务必要的剧本、角色、场景、地图、资产和连续性。
5. 回答前检查剧情、角色、知识边界、空间、资产、揭示预算和连续性冲突。
6. 回答后扫描稳定学习、正式资产、剧情决定和生产状态变化。
7. 需要正式写回时执行 `FETCH → EDIT → COMMIT → FETCH VERIFY`。
8. 只有回读验证成功才允许说“已写入 / 已登记 / 已修改”。

“每轮生效”不等于全文加载。默认采用相关章节局部检索；只有全局审计、复杂跨域失败或路由置信度不足时扩大读取范围。

# 二、文字与媒体职责

## GitHub：持续成长文字真相源

固定文件名，不建立 `v1.1 / v1.2 / final` 文件森林。版本历史由 Git commit/tag 管理。

主要职责：

- AI电影系统
- AI电影项目记忆
- 当前改编剧本
- 角色与表演设定
- 场景与空间设定
- 文字地图
- 正式资产登记和默认指针
- 连续性与当前生产状态
- 反馈学习与经验治理
- 官方资料与证据索引
- Director 路由、回归案例、Unknown Registry

## ChatGPT Library：大型媒体与只读证据

主要保存：

- 角色与场景图片
- 视频、音频
- 白模、深度图
- 原作母本与大型文本
- PDF、官方原始资料
- 历史快照

GitHub 中只保存这些媒体的 Asset ID、File ID、状态、职责、默认指针、连续性绑定和确认记录。

## Memory

Memory 只能做辅助提醒和快速索引，不得覆盖 GitHub / 当前 canonical 的正式事实。

# 三、剧情裁决顺序

```text
用户当前明确确认
→ 当前改编剧本
→ 已确认母本改动/剧情资产
→ 原作母本与检索索引
→ 角色 / 场景 / 地图 / 连续性资料
→ AI电影系统
→ 一般知识
```

# 四、资产裁决顺序

```text
用户本次明确指定
→ 当前镜头连续性绑定的正式资产
→ 正式默认资产
→ 用户确认的合法备选
→ 无正式资产时明确报告缺失
```

用户说“登记资产 / 录入资产 / 加入资产库 / 设为正式资产”时，默认就是正式 `active` 入库请求；除非用户明确说候选、临时或待审核。

登记完成必须回读正式 Asset Registry 验证，Memory 不得冒充资产库。

# 五、Source Authority Gate

以下旧文件默认拒绝作为活动规则源：

```text
AI电影化系统总纲_v*.md
AI电影化系统总纲.md
*_更新补丁.md
```

文件正文自称 `active`、搜索相关度、创建时间或修改时间，都不能推翻 `PROJECT_INDEX.yaml`。

# 六、持续成长闭环

```text
PRE-READ
→ DIRECT
→ VALIDATE
→ 用户反馈 / 真实生成结果
→ Final-Delta / 反事实
→ candidate / verified 分级
→ 写回唯一目标主档
→ 回归案例
→ 下一轮按需召回
```

稳定规则采用成熟度：

`candidate → scene_verified → project_verified → general_stable`

并保留：`conflicted / needs_revalidation / deprecated`。

# 七、迁移期规则

- 迁移状态以 `MIGRATION_STATUS.md` 为准。
- 一个文件只有完成“完整读取旧 canonical → GitHub 写入 → GitHub 回读核验”后，才能把 effective source 切换为 GitHub。
- 未完成的继续读 File Library 当前 canonical。
- 禁止根据摘要或记忆重建长主档后直接宣布迁移完成。
